apiVersion: v1
kind: Service
metadata:
  name: todo-api-service
  namespace: hch-api-project
spec:
  selector:
    app: todo-api-quarkus
  ports:
    - protocol: TCP
      port: 80
      targetPort: http
